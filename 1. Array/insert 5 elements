#include <stdio.h>

int main() {
    int no[5];
    for (int i = 0; i < 5; i++) {
        printf("Enter number %d: ", i + 1);
        scanf("%d", &no[i]);
    }
    for (int i = 0; i < 5; i++) {
        printf("Number %d: %d\n", i + 1, no[i]);
    }
    return 0;
}
